# Monorepos & Micro-Frontends

Use Nx for monorepos and Module Federation for MFEs.

```bash
npx create-nx-workspace@latest
```
**Expected output:** Workspace with shared libs and enforceable boundaries; MFEs federate routes and features.
