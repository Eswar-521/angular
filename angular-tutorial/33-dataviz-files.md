# Charts, Tables & Files

Use Chart.js/ngx-charts, AG Grid, CDK virtual scroll, and signed URLs for uploads.

```html
<input type="file" (change)="onFile($event)" />
```
**Expected output:** Large tables stay smooth with virtual scroll; secure uploads via S3/GCS presigned URLs.
