# PWA & Offline

Install Angular's service worker and configure caching.

```bash
ng add @angular/pwa
```
**Expected output:** App gains a manifest, icons, and offline caching; enable push with Web Push APIs.
