# Styling & UI Libraries

Use view encapsulation, Tailwind, and Angular Material/CDK smartly.

```ts
// tailwind.config.js (example)
module.exports = { content: ['./src/**/*.{html,ts}'], theme: { extend: {} }, plugins: [] }
```
**Expected output:** Utility classes usable in templates; component styles scoped unless overridden.
