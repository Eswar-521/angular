# Internationalization (i18n)

Use the built-in i18n extraction and runtime locale data.

```html
<!-- template -->
<p i18n="@@welcome-msg">Welcome</p>
```
**Expected output:** Extraction generates messages; translated bundles swap text per locale.
