# Migrations & Upgrades

Use `ng update` and codemods; convert to standalone and adopt signals gradually.

```bash
ng update @angular/core @angular/cli
```
**Expected output:** Project updates framework packages and applies required migrations safely.
